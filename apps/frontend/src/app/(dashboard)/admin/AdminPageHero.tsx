/** Same hero treatment as the student dashboard — re-export to keep admin styling aligned. */
export { default } from "@/app/(dashboard)/student/StudentPageHero";
export type { StudentPageHeroProps as AdminPageHeroProps } from "@/app/(dashboard)/student/StudentPageHero";
