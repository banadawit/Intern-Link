/** Same hero treatment as student & admin dashboards — keeps HOD headers visually aligned. */
export { default } from "@/app/(dashboard)/student/StudentPageHero";
export type { StudentPageHeroProps as HodPageHeroProps } from "@/app/(dashboard)/student/StudentPageHero";
