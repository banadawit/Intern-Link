/** Dispatched from WeeklyPlans when `plans` changes so the notification UI can stay in sync. */
export const STUDENT_WEEKLY_PLANS_EVENT = "internlink-student-weekly-plans";

/** localStorage key for which plan-activity notice ids the student has already seen (read). */
export const STUDENT_PLAN_NOTICES_READ_KEY = "internlink-student-plan-notices-read";
